name: hikot
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  
  # Use the local path ONLY
  flutter_nearby_connections:
    path: plugins/flutter_nearby_connections-1.1.2

  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^1.31.0
  sqflite: ^2.4.1
  shared_preferences: ^2.5.2
  maplibre_gl: ^0.26.0
  geolocator: ^14.0.2
  collection: ^1.19.1
  uuid: ^4.5.1
  vibration: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/hiker_welcome.png
    - assets/hiker_iot_device.jpg
    - assets/climber_iot_device.png

